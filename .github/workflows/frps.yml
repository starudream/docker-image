name: frps
on:
  push:
    branches:
      - master
    paths:
      - '.github/workflows/frps.yml'
      - 'frps/**'
  schedule:
    - cron: '0 2 * * *'
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          fetch-depth: 1
      - name: Build
        run: |
          cd frps
          echo "${{secrets.DOCKER_TOKEN}}" | docker login -u starudream --password-stdin
          docker build -t starudream/frps:latest .
          docker push starudream/frps:latest
